name: WiraAvia Auto Update

on:
  schedule:
    # Berjalan otomatis setiap menit ke-10 dan ke-40 setiap jam (Memberi waktu BMKG rilis data)
    - cron: '10,40 * * * *'
  workflow_dispatch: # Memunculkan tombol agar bisa dijalankan manual kapan saja

jobs:
  update-map:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas folium requests urllib3
          
      - name: Jalankan AI WiraAvia
        env:
          MY_GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
          TELE_BOT_TOKEN: ${{ secrets.TELE_BOT_TOKEN }}
          TELE_CHAT_ID: ${{ secrets.TELE_CHAT_ID }}
        run: python main.py
